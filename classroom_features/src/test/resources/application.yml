server:
  port: 8081
  servlet:
    context-path: /qr_code
spring:
  thymeleaf:
    cache: false
    check-template-location: true
    servlet:
      content-type: text/html
    enabled: true
    encoding: utf-8
    mode: HTML5
    prefix: classpath:/templates/
    suffix: .html

mysql:
  datasource:
    driver-Class-Name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/project?useUnicode=true&characterEncoding=UTF-8&connectTimeout=60000&socketTimeout=60000&tinyInt1isBit=false&serverTimezone=GMT%2B8
    username: root
    password: rootroot